<main>
  <p><%= flash[:notice] %></p>

  <div class='container px-5 px-sm-0'>
      <div class="errors_explanation">
        <% if book.errors.any? %>
            <h3><%= book.errors.count %> prohibited this obj from being saved:</h3>
            <ul>
                <% book.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        <% end %>
      </div>
    <div class='row'>
       <div class='col-md-3'>
         <h2>User info</h2>
         <table class='table'>
            <tr>
                <% if current_user == user.id %>
                <th><%= image_tag current_user.get_profile_image(100,100) %></th>
            <tr>
                <th>name</th>
                <th><%= current_user.name %></th>
            </tr>
            <tr>
                <th>introduction</th>
                <th><%= current_user.introduction %></th>
            </tr>
               　<% else %>
                <th><%= image_tag user.get_profile_image(100,100) %></th>
            <tr>
                <th>name</th>
                <th><%= user.name %></th>
            </tr>
            <tr>
                <th>introduction</th>
                <th><%= user.introduction %></th>
            </tr>
                <% end %>
         </table>
         <div class="row">
             　<%= link_to edit_user_path(user.id), class: "btn btn-outline-secondary btn-block" do %>
                 <i class="fas fa-user-cog"></i>
            　　 <% end %>
         </div>
      　  <h2 class="mt-3">New book</h2>
      　  <%= form_with model: book, url: '/books' , method: :post, html: {autocomplete: "off"} do |f| %>
      　    <div class="form-group">
        　    <label for="book_title">Title</label>
        　    <%= f.text_field :title, class: "form-control book_title",autocomplete: "off" %>
      　    </div>
        　  <div class="form-group">
        　    <label for="book_opinion">Opinion</label>
        　    <%= f.text_area :body, class: "form-control book_body", autocomplete: "off" %>
        　  </div>
        　  <div class="form-group">
          　  <%= f.submit "Create Book",class: "btn btn-success" %>
        　  </div>
      　  <% end %>


      </div>

