<%= render "books/new",user: @user, book: @book%>

    <div class='col-md-8 offset-md-1'>
        <h2>Books</h2>
        <table class="table table-hover table-inverse">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Opinion</th>
                    <th colspan="3"></th>
                </tr>
            </thead>
            <% @books.each do |book| %>
            <tbody>
                <tr>
                    <td>
                        <%= link_to book_path(book.id) do%>
                        <%= image_tag("default-image.jpg", size: "100×100") %>
                        <%= book.title %>
                        <% end %>
                    </td>
                    <td><%= book.body %></td>
                </tr>
            </tbody>
            <% end %>
        </table>
    </div>

    </div>
  </div>
</main>